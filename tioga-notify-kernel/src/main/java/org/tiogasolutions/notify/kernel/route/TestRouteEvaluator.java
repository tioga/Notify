package org.tiogasolutions.notify.kernel.route;

public class TestRouteEvaluator {

  public TestRouteEvaluator() {

/*
    List<Route> localRoutes = new ArrayList<>();

    Map<String, Object> args = new HashMap<>();
    args.put("type", "email");
    args.put("recipients", Arrays.asList("a@test.com", "b@test.com"));
    Destination destination = new Destination("local", "push", args);
    String evalFunc = "function(topic, traits) {return true;}";
    Route route = new Route("simpleRoute", evalFunc, Collections.singletonList(destination));

    localRoutes.add(route);

    routes = Collections.unmodifiableList(localRoutes);

    routeEvaluator = new RouteEvaluator(routes);
*/


  }

}
